import React, { View, Text } from 'react-native'
import { shallow, mount, render } from 'enzyme';
import { expect } from 'chai'

describe('Up and running', () => {
  it('Mocha, Chai-Enzyme, Enzyme', () => {

  })
})
